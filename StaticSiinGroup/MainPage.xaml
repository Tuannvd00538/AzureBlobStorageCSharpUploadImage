<Page
    x:Class="StaticSiinGroup.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StaticSiinGroup"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Upload Image To Azure Blob Storage" FontWeight="Bold" FontSize="26"/>
            <RelativePanel HorizontalAlignment="Center">
                <Button x:Name="ChooseAvatar" Content="Choose File" HorizontalAlignment="Center" Margin="0,20,0,0" VerticalAlignment="Top" Width="400" Click="Choose_Avatar"/>
            </RelativePanel>
            <RelativePanel HorizontalAlignment="Center">
                <Image x:Name="PreviewAvatar" Margin="0,20" Width="230"/>
            </RelativePanel>
            <TextBlock x:Name="UrlUpload"/>
        </StackPanel>
    </ScrollViewer>
</Page>
